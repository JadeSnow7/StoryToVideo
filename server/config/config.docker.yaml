server:
  port: ":8080"

mysql:
  dsn: "root:12345678@tcp(mysql:3306)/storydb2?charset=utf8mb4&parseTime=True&loc=Local"

ai:
  # Model service endpoints (container network)
  image_api: "http://model:8000/txt2img"
  voice_api: "http://model:8000/tts"

redis:
  addr: "redis:6379"
  password: ""
  db: 0

worker:
  # Gateway endpoint (container network)
  addr: "http://gateway:8000"
  timeout_minutes: 20

minio:
  # MinIO endpoint (container network)
  endpoint: "minio:9000"
  access_key: "story2video"
  secret_key: "s2v_secret"
  bucket: "story2video"
  use_ssl: false
  # External domain for client access
  domain: "http://localhost:9000"
